server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://postgres-identity:5432/identity_db
    username: karima
    password: karima123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect